# Replace OWNER after publishing the image.
IMAGE=ghcr.io/OWNER/agentic-workflow-dev-env:latest
NODE_RED_PORT=1880
TZ=UTC

# Prefer a fine-grained, least-privilege GitHub token for private repositories.
GH_TOKEN=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
